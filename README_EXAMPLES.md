Example usage and testing tips:
- Use a lab VM running simple web auth (e.g., httpbin's basic-auth endpoint) for bruteforce tests.
- Use a local server (nc -l) to test open/closed ports.
- Always run against authorized targets.
